const DeleteButton = () => {
    // text OFF => ON
    // text ON => OFF
    const onClick = () => {
    }
    return (
        <div>
            <button onClick={onClick} className="deletebtn"> -1 </button>
        </div>
    );
}
export default DeleteButton;